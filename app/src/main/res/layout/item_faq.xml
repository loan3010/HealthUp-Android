<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:cardBackgroundColor="@color/neutral_white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <RelativeLayout
            android:id="@+id/layoutQuestion"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/tvQuestion"
                style="@style/TextAppearance.HealthUp.Content"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_toStartOf="@id/ivToggle"
                android:text="Làm thế nào để theo dõi đơn hàng?"
                android:textColor="@color/neutral_black" />

            <ImageView
                android:id="@+id/ivToggle"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_arrow_down"
                app:tint="@color/neutral_grey" />
        </RelativeLayout>

        <TextView
            android:id="@+id/tvAnswer"
            style="@style/TextAppearance.HealthUp.SubContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Bạn có thể theo dõi đơn hàng bằng cách vào phần Tài khoản -> Lịch sử mua hàng..."
            android:textColor="@color/neutral_dark_grey"
            android:visibility="gone" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
